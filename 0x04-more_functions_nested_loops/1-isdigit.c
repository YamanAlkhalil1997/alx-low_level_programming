#include "main.h"

/**
 * _isuupper - check if c is upper
 *
 * @c imput for alphabet
 *
 * return: 1 if its uppercase, 0 if not
*/

int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
		return (1);
	else
		return (0);
}
